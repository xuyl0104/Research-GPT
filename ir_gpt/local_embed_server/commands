<!-- tutorials -->
https://chatgpt.com/share/680c0d10-c1f0-8012-98bd-4287b5a63874

<!-- pip install -r requirements.txt
uvicorn server:app --host 0.0.0.0 --port 8000

curl http://localhost:8000/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{"input": "The meaning of life is..."}' -->


--- using docker
- In your docker file folder:

docker-compose build
docker-compose up -d


docker-compose down // for stopping in wsl2

docker-compose up -d  // for restarting in wsl2

watch -n 2 nvidia-smi  // monitor GPU usage in wsl2
